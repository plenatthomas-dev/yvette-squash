# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# vercel (lien projet local — ne pas committer)
/.vercel

# typescript build info
*.tsbuildinfo

# production
/build

# env & secrets — NE JAMAIS committer
.env
.env*.local

# captures réseau (contiennent mot de passe + jetons)
*.har

# base de données locale
/prisma/dev.db
/prisma/dev.db-journal

# logs / divers
npm-debug.log*
*.log
.DS_Store

# revue de code (document de travail, ne pas committer)
revue-code-yvette-squash.pdf

# rapports d'audit sécurité — NE JAMAIS committer : le dépôt distant est PUBLIC et ces
# documents dressent la carte des failles encore ouvertes (et citent en clair un secret
# déjà fuité). Les garder hors de l'arbre suivi, pas seulement hors du dernier commit.
Sec_Arch_analysis_Gemini.md
yvette-squash-analyse-securite.md
.vercel
.env*

# impeccable — le live mode écrit des artefacts d'EXÉCUTION dans .impeccable/live/
# (journal d'injection, état de session). Seule sa config est versionnée.
/.impeccable/live/*
!/.impeccable/live/config.json

# Claude Code — config LOCALE (permissions propres à la machine). La config partagée
# .claude/settings.json, elle, reste versionnée.
.claude/settings.local.json
